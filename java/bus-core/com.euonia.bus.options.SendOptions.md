# SendOptions
> 发送/命令模式的选项类，继承自 `ExtendableOptions`，额外增加了 `correlationId` 属性。
- **Type**: class
- **Package**: `com.euonia.bus.options`
- **Author**: (not specified)

## Fields
| Field | Type | Description |
|-------|------|-------------|
| `correlationId` | `String` | 关联标识符 |

## Methods
### getCorrelationId
> 获取关联标识符。
- **Returns**: `String` - 关联标识符

### setCorrelationId
> 设置关联标识符。
- **Parameters**:
  - `correlationId` (`String`): 关联标识符
