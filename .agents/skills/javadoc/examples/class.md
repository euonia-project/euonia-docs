# 类名

> 这里是类的描述信息，如果描述信息中出现Java关键字或者其他类名、参数名、方法名或者代码，一律使用行内代码包裹。

- **Type**: `class`、`abstract class`等
- **Package**: `package_name`
- **Author**: 作者信息，如果没有则使用damon(zhaorong@outlook.com)
- **Extends**: `Extends`
- **Implements**: `interface name`

## Type Parameters

| Parameter | Bound | Description |
|-----------|-------|-------------|
| `Parameter name` | `Parameter Bound` | Parameter description |

## Annotations

### Annotation1

> 注解1描述

- `value`：value
- `element1` element1 value

## Fields

| Field | Type | Description |
|-------|------|-------------|
| `Field Name` | `Field Type` | Field Description |

## Methods


### 方法名1

> 方法的注释，规则与类注释一致

- **Parameters**:
  - `parameter1` (`parameter1 type`): parameter1 注释
  - `parameter2` (`parameter2 type`): parameter2 注释
- **Returns**: `Return Type` - 返回值注释，如果返回值类型是void则不显示此行
- **Type Parameters**：
  - `TypeParameter1` (`TypeParameter1 Bound`): TypeParameter1 注释
- **Throws**: `IllegalArgumentException` - 如果对象为 null

### 方法名2

> 方法的注释，规则与类注释一致

- **Parameters**: （无）